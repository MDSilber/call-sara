"""The advisor layer: every derived number, check, and artifact — computed
once here, consumed by every surface (CLI entrypoints, the server, the
letter). Depends on sara.vault / sara.rules / sara.ledger; nothing here may
import sara.server."""
